# Caddy reverse proxy: automatic HTTPS. All requests are proxied to FastAPI,
# which serves the dashboard (templates + static) and the JSON API.

{$DOMAIN} {
	encode gzip zstd
	reverse_proxy api:8000
}

